# tools/watermark.py

Not part of the site build — internal use only for sending client preview images.

```
pip install -r tools/requirements.txt
python tools/watermark.py path/to/preview.jpg -o path/to/preview-watermarked.jpg
```
